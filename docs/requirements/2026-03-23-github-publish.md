# 需求文档：将当前项目上传到 GitHub 并补充项目说明

- 日期：2026-03-23
- 项目：Bookmark WebDAV Sync
- 仓库目录：`D:\A-02-APP\05-浏览器扩展\书签同步`

## 1. 目标
将当前浏览器扩展项目上传到 GitHub，并补充适合公开展示与后续维护的项目说明文档。

## 2. 交付物
- 一个已创建并成功推送的 GitHub 仓库
- 本地仓库已配置 `origin`
- 新增或补充的 `README.md`
- 受控运行产物：需求文档、执行计划、运行回执、清理回执

## 3. 约束
- 使用当前已登录的 GitHub CLI 账号 `Godwin9527`
- 采用推荐方案：新建仓库、可见性为 `Public`
- 推荐仓库名：`bookmark-webdav-sync-extension`
- 不做与本次目标无关的代码功能改动
- 不推测用户未授权的外部目标，只创建并推送到本用户账号下的新仓库

## 4. 验收标准
- `gh repo create` 成功并返回仓库信息
- `git remote -v` 可看到正确的 `origin`
- `git push -u origin master` 成功
- `README.md` 包含：项目简介、功能特性、安装使用、开发说明、截图占位
- 输出本次仓库链接

## 5. 非目标
- 不修改扩展核心逻辑
- 不发布到 Chrome Web Store
- 不补充真实截图资源

## 6. 自主执行约定
- 已由用户确认“按推荐方案执行”
- 允许创建 GitHub 仓库并推送当前项目

## 7. 推断假设
- 当前工作区内容即为用户希望上传的完整项目
- `master` 作为当前默认推送分支可接受
- 项目适合公开展示，不包含待排除的敏感文件
